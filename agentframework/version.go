package agentframework

// Version is the version of this module. The release workflow reads it to
// derive the agentframework/v<version> tag, so it is the single declaration of
// what the next agentframework release will be called.
const Version = "0.1.0"
